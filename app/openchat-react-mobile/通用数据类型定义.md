# AI时代通用 MediaResource 资源存储结构标准

> 本文档定义一套**面向人工智能时代（AI-Native）**的通用资源存储与引用数据结构标准（MediaResource Standard，简称 **MRS**）。
>
> 该标准适用于：
>
> * 🎬 剪辑系统 / 非线性编辑器（NLE）
> * 🤖 AI Agent / AI Workflow / Agent Skills
> * 🧠 知识库 / 素材库 / 内容管理系统（CMS）
> * ☁️ 云端协同系统 / 多模态系统
>
> **核心目标**：
>
> * 把“资源”从「URL / 文件」升级为「**可生成、可重写、可演化的 AI 资源实体**」
> * 支持 **随时修改 Prompt → 重新生成资源**
> * 支持 **跨系统、跨端、跨 AI 引擎复用**

---

## 1. 设计哲学（Design Philosophy）

### 1.1 为什么不能只用 URL？

传统系统：

```text
Asset = URL / FilePath
```

在 AI 时代存在致命缺陷：

* ❌ 无法表达“生成来源”
* ❌ 无法追溯 Prompt / 参数
* ❌ 无法再生成 / 变体生成
* ❌ 无法支持多模态融合（图 + 音 + 人设）

### 1.2 AI-Native 资源观

> **资源不是文件，而是「生成能力 + 状态快照」**

因此我们引入：

> ✅ **MediaResource = AI 时代的最小资源原语**

---

## 2. 核心抽象：MediaResource

### 2.1 资源生命周期模型

```text
Prompt / Input
   ↓
AI Generator
   ↓
MediaResource (State)
   ↓
Re-Prompt / Re-Generate / Versioning
```

MediaResource 本身：

* 可以是「生成结果」
* 也可以是「生成中间态」
* 也可以只是「生成描述（Prompt）」

---

## 3. 媒体资源类型枚举

```ts
export enum MediaResourceType {
  IMAGE = 'IMAGE',
  VIDEO = 'VIDEO',
  AUDIO = 'AUDIO',
  DOCUMENT = 'DOCUMENT',
  FILE = 'FILE',
  MUSIC = 'MUSIC',
  CHARACTER = 'CHARACTER',
  MODEL_3D = 'MODEL_3D',
  PPT = 'PPT',
  CODE = 'CODE'
}
```

📌 **设计原则**：

* 类型 ≠ 存储方式
* 类型 = 语义 + AI 处理方式

---

## 4. MediaResource 基础结构（核心标准）

```ts
export interface MediaResource {
  id?: string;
  uuid?: string;

  // === 数据载体 ===
  url?: string;
  bytes?: number[];
  base64?: string;
  localFile?: object;

  // === 基础描述 ===
  type?: MediaResourceType;
  mimeType?: string;
  size?: string;
  name?: string;
  extension?: string;

  // === AI 语义 ===
  prompt?: string;                // 可修改、可再生成
  metadata?: Record<string, any>; // 模型参数 / 生成配置

  // === 组织与治理 ===
  tags?: TagsContent;
}
```

### 4.1 关键设计说明

| 字段           | AI 语义 | 说明          |
| ------------ | ----- | ----------- |
| prompt       | ⭐⭐⭐⭐⭐ | 资源的“灵魂”，可重写 |
| metadata     | ⭐⭐⭐⭐  | 模型参数、采样方式   |
| url / base64 | ⭐⭐    | 只是当前状态      |

---

## 5. 标签体系（TagsContent）

```ts
export interface TagsContent {
  tags?: string[];
  children?: TagsContent[];
}
```

支持：

* 🌳 树状标签
* 🤖 AI 分类 / AI 聚类
* 🔎 多系统统一索引

---

## 6. 媒体资源细分规范

### 6.1 视频资源（VideoMediaResource）

```ts
export interface VideoMediaResource extends MediaResource {
  duration?: string;
  width?: string;
  height?: string;
}
```

适用：

* 剪辑系统
* AI 视频生成
* 数字人驱动视频

---

### 6.2 图片资源（ImageMediaResource）

```ts
export interface ImageMediaResource extends MediaResource {
  width?: string;
  height?: string;
  aspectRatio?: string;
  splitImages?: ImageMediaResource[];
}
```

支持：

* AI 图像分割
* 多图变体
* 重绘（inpainting / outpainting）

---

### 6.3 音频 / 音乐资源

```ts
export interface AudioMediaResource extends MediaResource {
  format?: AudioFormat;
  bitRate?: string;
  sampleRate?: string;
  channels?: string;
  duration?: string;
}
```

用于：

* TTS
* 配音
* 背景音

---

### 6.4 数字人 / 角色资源（CharacterMediaResource）

```ts
export interface CharacterMediaResource extends MediaResource {
  characterType?: string;
  gender?: string;
  ageGroup?: string;
  avatarUrl?: string;
  avatarVideoUrl?: string;
  speakerId?: string;
  appearanceParams?: Record<string, any>;
  animationParams?: Record<string, any>;
}
```

📌 **这是 AI 时代的核心资源类型之一**：

* 数字主播
* AI 虚拟员工
* Agent Avatar

---

## 7. 资源包装器（AssetMediaResource）

```ts
export interface AssetMediaResource extends MediaResource {
  image?: ImageMediaResource;
  video?: VideoMediaResource;
  audio?: AudioMediaResource;
  music?: MusicMediaResource;
  character?: CharacterMediaResource;
  file?: FileMediaResource;
  extraProps?: Record<string, any>;
}
```

用途：

* 多模态资源聚合
* AI Agent 输入输出
* 跨系统兼容

---

## 8. 联合类型定义

```ts
export type AnyMediaResource =
  | FileMediaResource
  | VideoMediaResource
  | ImageMediaResource
  | AudioMediaResource
  | MusicMediaResource
  | CharacterMediaResource
  | AssetMediaResource;
```

---

## 9. 强制设计约束（必须遵守）

### 9.1 资源不可被“直接替换”

* ❌ 不允许直接覆盖 url
* ✅ 必须通过 Prompt / Metadata 重新生成

### 9.2 AI 系统通用约束

* MediaResource 必须可序列化
* 必须支持无文件态（仅 Prompt）
* 必须可作为 Agent Memory

---

## 10. 跨系统适配能力

| 系统       | 适配能力  |
| -------- | ----- |
| 剪辑系统     | ⭐⭐⭐⭐⭐ |
| AI Agent | ⭐⭐⭐⭐⭐ |
| CMS      | ⭐⭐⭐⭐  |
| 知识库      | ⭐⭐⭐⭐  |
| 多模态搜索    | ⭐⭐⭐⭐⭐ |

---

## 11. 总结（核心一句话）

> **MediaResource 不是“素材”，而是 AI 时代的“可生成内容实体”**。
>
> 这套标准，可以作为：
>
> * AI 产品的底层资源协议
> * Agent 世界的通用资产规范
> * 下一代内容系统的基础设施

--- 
